<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="publicationDao">


	<select id="selectPubliList"  resultType="egovMap" parameterType="map" >
		 SELECT "no", title, "content", author, regdt, updt, eng_title, eng_content, imgnm, filenm, linkurl, citation
			FROM public."lettnbbs" where 1=1
			<if test="no != null">
			and no = #{no}
			</if>
			<if test="search != null">
			and (title like  '%'||#{search}||'%' or content like  '%'||#{search}||'%')
			</if>
			 order by "no" desc
	</select>

</mapper>